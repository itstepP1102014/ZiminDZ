#include <stdio.h>
#include <stdlib.h>
#include <math.h>


int main()

{
    double x;
    int p;
    printf("Enter a number: ");
    scanf("%lf", &x);
    printf("Enter a stage of number: ");
    scanf("%d", &p);
    printf("%lf ^ %d = %lf", x, p, stage);

return 0;
}
    int i;
    double stage = 1.0;
    for (int i = 1; i <= abs(p); i++)
    {
        stage *= x;
    }
    return p
